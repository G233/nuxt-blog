const Goods = require('./mod/lei.model')
const User = require('./mod/user.model')
const Article = require('./mod/article.model')
const Label = require('./mod/label.model')

// const Inlog = require('./mod/inlog.model')
// const Outlog = require('./mod/outlog.model')

module.export = {
  User,
  Article,
  Label,
  // Inlog,
  // Outlog,
  Goods
}